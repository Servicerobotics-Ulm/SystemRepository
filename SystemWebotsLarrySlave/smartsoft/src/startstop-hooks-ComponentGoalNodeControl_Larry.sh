#!/bin/bash
#
# Start-Stop-Hooks for component ComponentGoalNodeControl_Larry
#
# This script provides methods to call custom commands pre/post of
# starting/stoping the component during launch on the device.
# This script is being executed on the target device where this 
# component is running.
#

#--------------------------------------------------------------------------
# Code generated by the SmartSoft MDSD Toolchain
# The SmartSoft Toolchain has been developed by:
#  
# Service Robotics Research Center
# University of Applied Sciences Ulm
# Prittwitzstr. 10
# 89075 Ulm (Germany)
#
# Information about the SmartSoft MDSD Toolchain is available at:
# www.servicerobotik-ulm.de
#
# This file is generated once. Modify this file to your needs. 
# If you want the toolchain to re-generate this file, please 
# delete it before running the code generator.
#--------------------------------------------------------------------------

case "$1" in

pre-start)
	# first, trigger hooks as delivered from component ...
	bash startstop-hooks-component-ComponentGoalNodeControl.sh pre-start
	
	# ...then call user/deployment specific hook:
	echo "Triggering pre-start hooks ..."
		sed -i -z 's/wiringName ShortestPathRequestServiceReq\nserverName unknown\nserviceName unknown\ninitialConnect false/wiringName ShortestPathRequestServiceReq\nserverName ComponentNavigationGraph\nserviceName ShortestPathRequestServiceAnsw\ninitialConnect true/g' ComponentGoalNodeControl_Larry.ini
		
				sed -i -z 's/wiringName NodeStatusUpdateServiceReq\nserverName unknown\nserviceName unknown\ninitialConnect false/wiringName NodeStatusUpdateServiceReq\nserverName ComponentNavigationGraph\nserviceName NodeStatusUpdateServiceAnsw\ninitialConnect true/g' ComponentGoalNodeControl_Larry.ini
			
					sed -i -z 's/wiringName ClosestFreeNodeServiceReq\nserverName unknown\nserviceName unknown\ninitialConnect false/wiringName ClosestFreeNodeServiceReq\nserverName ComponentNavigationGraph\nserviceName ClosestFreeNodeServiceAnsw\ninitialConnect true/g' ComponentGoalNodeControl_Larry.ini	
				
;;

post-start)
	# first, trigger hooks as delivered from component ...
	bash startstop-hooks-component-ComponentGoalNodeControl.sh post-start
	
	# ...then call user/deployment specific hook:
	echo "Triggering post-start hooks ..."
;;

pre-stop)
	# first, trigger hooks as delivered from component ...
	bash startstop-hooks-component-ComponentGoalNodeControl.sh pre-stop
	
	# ...then call user/deployment specific hook:
	echo "Triggering pre-stop hooks ..."
;;

post-stop)
	# first, trigger hooks as delivered from component ...
	bash startstop-hooks-component-ComponentGoalNodeControl.sh post-stop
	
	# ...then call user/deployment specific hook:
	echo "Triggering post-stop hooks ..."
;;

*)
	echo "ERROR in $0: no such hook '$1'. Usage: $0 pre-start|post-start|pre-stop|post-stop"
;;

esac

